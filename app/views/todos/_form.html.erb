<h1>Nueva Venta</h1>

  <div class="row justify-content-md-center col-md-10">
    <div class="col-md-5">
      <div class="row justify-content-md-center">
        <%= form_with model: @todo, url: add_path, local: true do |f| %>
        <div class="form-group col-md-10">
          <%= f.label :description, "ToDo" %>
          <%= f.text_field :description, class: 'form-control'%>
        </div>
     
        
        <div class="form-group col-md-5">
          <%= f. label :completed, "completed¿?" %>
          <%= f.check_box :completed %>
          <%= f.submit "Crear Venta", class: 'btn btn-primary' %>

        </div>
        <div class="form-group col-md-5">
          <%= link_to 'Volver', todos_path, class: 'btn btn-success' %>
        </div>
        <% end %>

      </div>
    </div>
  </div>